% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_bar.R
\name{plot_bar}
\alias{plot_bar}
\title{Plot a Bar Plot}
\usage{
plot_bar(
  data,
  score,
  metric = "index",
  test,
  group,
  ci.lb,
  ci.ub,
  axis.label.metric = metric,
  color = "black",
  fill = "white",
  z.lines = TRUE,
  z.line.color = "black",
  descriptors = TRUE,
  descriptors.color = "black",
  shading = TRUE,
  shading.color = "#2fa4e7",
  abbreviations = FALSE
)
}
\arguments{
\item{data}{A data frame containing the data to be plotted.}

\item{score}{The column name in the data frame representing the scores to be
plotted.}

\item{metric}{The metric of the scores in the data frame. Defaults to "z".
The metric can be converted to "z" and other supported metrics.}

\item{test}{The column name in the data frame representing the test names.}

\item{group}{The column name in the data frame representing the group names.}

\item{ci.lb}{(Optional) The column name in the data frame representing the
lower bound of the confidence interval.}

\item{ci.ub}{(Optional) The column name in the data frame representing the
upper bound of the confidence interval.}

\item{axis.label.metric}{The metric to be used for axis labels. Defaults to
the value of \code{metric}. Can be "percentile", "t", "index", "scaled", or "z".}

\item{color}{The color of the points and error bars. Defaults to "black".}

\item{fill}{The fill color of the bars. Defaults to "white".}

\item{z.lines}{(Optional) Logical value indicating whether to draw lines at
z-scores from -4 to 4. Defaults to TRUE.}

\item{z.line.color}{(Optional) Color of the z-lines. Defaults to "black".}

\item{descriptors}{(Optional) Logical value indicating whether to include
descriptive labels on the plot. Defaults to TRUE.}

\item{descriptors.color}{(Optional) Color of the descriptor labels. Defaults to "black".}

\item{shading}{(Optional) Logical value indicating whether to include shading
on the plot for the average range. Defaults to TRUE.}

\item{shading.color}{(Optional) Color of the shaded area. Defaults to "#2fa4e7".}

\item{abbreviations}{(Optional) Logical value indicating whether to abbreviate
test names. Defaults to FALSE.}
}
\value{
A ggplot object representing the Bar plot.
}
\description{
Creates a Bar plot visualizing scores and their confidence intervals
across different tests and groups. The plot includes optional shading and
descriptors to provide additional context.
}
\examples{
# Example data frame
df <- data.frame(
  test = rep(c("Test1", "Test2", "Test3"), 2),
  group = rep(c("Group1", "Group2"), each = 3),
  score = c(1.2, 2.3, 1.5, -1.2, 0.5, 1.1),
  ci.lb = c(0.8, 1.8, 1.2, -1.5, 0.2, 0.8),
  ci.ub = c(1.6, 2.8, 1.8, -0.9, 0.8, 1.4)
)

# Plot with default settings
plot_bar(df, score = score, test = test, group = group, ci.lb = ci.lb, ci.ub = ci.ub)

# Plot with no descriptors and shading
plot_bar(df, score = score, test = test, group = group, ci.lb = ci.lb, ci.ub = ci.ub,
          descriptors = FALSE, shading = FALSE)
}
